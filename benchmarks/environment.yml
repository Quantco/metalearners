name: benchmarks
channels:
  - conda-forge
  - nodefaults
dependencies:
  ## Git
  - git_root
  ## Python
  - python=3.10
  # First version to add the return_indices parameter to cross_validate.
  - scikit-learn>=1.3
  - numpy
  - pandas
  - lightgbm
  - econml>=0.15
  - causalml
  - pip
  - tabulate
